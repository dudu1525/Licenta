!!!
dont have the usb on and also have the switch on to the esp board.

-when charging:
just usb into the tcp and the switch to be off!

-when using:
just remove the usb and power goes into the board.






workflow:

1. Mic captures sound

2. Mic sends sound to MC

3. Esp sends the captured sound to the phone its connected to (wifi or BLE).

4. Phone sends processed text back to the ESP

5. ESP sends text to OLED

6. OLED displays text.


---------------------problems:
how will someone see the text if its continosuly sent, how to scroll: text-wrapping algorithm



-------------connection:
-use wifi: (BLE not good if sending audio, not enough bandwith)
^esp connects to wifi hotspot (further upgrade: make it via BLE or with normal BL)
-phone acts as the web socket and triggers updates on the OLED
-in the loop, continuously send audio




--------------------application part

-first gets audio from the microphone of the phone
v1 of the app:
-API SDK: ANDROID NOUGAT 7.0 min version.
- just build it with gradle and vosk and see how it works on the phone, how the phone handles the apk and vosk

-----next step: import vosk dependencies

-build the app



--> then gets info from the audio of the MC







